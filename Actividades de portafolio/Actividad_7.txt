//Primer Algoritmo

Se almacena datos en la pila que son los siguientes: {1,2,3,4}
s= initialize //se inicializa la pila
while(no se ha obtenido el segundo elemento)
	lee los elementos (num)
	if(se encuentra el segundo)
		lee, almacena en una variable aparte  el elemento y luego se quita(pop) 

despliega la variable almacenada del segundo elemento de la pila


//Tercer Algoritmo

Se almacena datos en la pila que son los siguientes: {1,2,3,4}
s=initialize
while(mientras que la pila no este vacia)
	lee los elementos (num)
	if(se encuentra ultimo elemento)
		lee, almacena en una variable aparte el elemento y luego se quita(pop)

	if(vacia(s)) // verifica si la pila esta vacia

despliega la variable almacenada del ultimo elemento